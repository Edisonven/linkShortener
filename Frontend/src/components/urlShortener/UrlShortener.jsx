import "../urlShortener/urlShortener.css"

export default function UrlShortener() {
  return (
    <section className="url-shortener__container flex items-center justify-center h-full">
      <h1 className="text-base text-slate-800 sm:text-[60px] sm:font-semibold dark:text-white">Acorta tu URL</h1>
    </section>
  );
}
